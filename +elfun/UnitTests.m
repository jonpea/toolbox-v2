classdef UnitTests < matlab.unittest.TestCase
    
    properties (TestParameter)
    end
    
    methods (Test, ParameterCombination = 'exhaustive')

        % NB: todb() and fromdb() moved to +specfun
                
    end
    
end
